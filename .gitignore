# jekyll (legacy, removed once the Next.js site is confirmed live)
_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
Gemfile.lock
assets/libs/
vendor

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# raw font downloads (source zips/installers, not the site's actual font
# files — those live under app/fonts/ and are tracked)
/fonts/

node_modules/
.idea
.DS_Store
*.pem